#include<iostream>
#include<cstdlib>

/*
typedef struct {
    int data;
    struct Node *next;
}Node;

Node * head;

int main() {
    head = (Node*)malloc(sizeof(Node));
    Node * node1 = (Node*)malloc(sizeof(Node));
    node1->data = 1;
    head->next = node1;
    Node * node2 = (Node*)malloc(sizeof(Node));
    node2->data = 2;
    node1->next = node2;
    node2->next = NULL;
    Node*cur = head->next;
    while(cur != NULL){
        printf("%d", cur->data);
    }
    return 0;
}*/